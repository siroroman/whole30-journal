CREATE TABLE ProgramEntity (
    startDate TEXT NOT NULL,
    durationDays INTEGER NOT NULL
);

select:
SELECT *
FROM ProgramEntity
LIMIT 1;

deleteAll:
DELETE FROM ProgramEntity;

insert:
INSERT INTO ProgramEntity (startDate, durationDays)
VALUES (?, ?);

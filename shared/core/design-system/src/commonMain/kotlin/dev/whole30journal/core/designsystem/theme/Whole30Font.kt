package dev.whole30journal.core.designsystem.theme

import androidx.compose.ui.text.font.FontFamily

// Placeholder pending approval to download the Inter TTF weights as compose resources. Swap the
// body for a FontFamily built from Res.font.inter_* — no call sites need to change.
fun whole30FontFamily(): FontFamily = FontFamily.Default
